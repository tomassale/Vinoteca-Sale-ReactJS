import { NavLink } from 'react-router-dom'
import { useContexto } from '../../context';

const Authgoogle = () => {
  
  const { signInGoogle, signOutGoogle, isSignIn } = useContexto()

  return(
    <>
      
    </>
  )
}

export default Authgoogle;
